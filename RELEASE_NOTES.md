# 知图 0.2.1 发布说明

> 发布日期：2026-09-17  
> 适用平台：Windows x64  
> 升级基线：v0.2.0

知图 0.2.1 是课程资料库交互修补版。本版为资料库增加直接多选入口，确保所选资料可靠传入 AI 生成流程；原有 AI 导图、资料解析、安全配置和导图编辑能力保持兼容。

## 0.2.1 更新

- 课程资料库增加复选框和“已选择 X/10 份”计数，不再需要修改底层会话数据。
- 选择状态保存到当前 AI 会话，并兼容缺少 `materialIds` 字段的旧会话。
- 切换“新建导图”和“补充选中节点”时保留已选资料。
- 单次最多选择 10 份资料；达到上限后禁用其他复选框，生成期间禁止修改选择。
- 主进程继续按资料 ID 读取并解析文件内容，渲染层不暴露文件绝对路径。
- 应用窗口、可执行文件、桌面与开始菜单快捷方式及 NSIS 安装器统一使用 `build/icon.ico`。
- 端到端测试验证被勾选资料正文进入生成请求，未选资料不会发送。

## 0.2.0 功能基线

### AI 导图助手

- 支持“新建导图”和“补充选中节点”两种模式。
- 支持 AI 追问、精简/标准/详尽三档规模、生成进度与中途取消。
- AI 结果先进入可编辑预览，可创建新导图，或追加、智能合并、替换现有子树。
- AI 会话保存在 `maps/<mapId>.ai-session.json`，不写入导图正文或导出文件。

### 课程资料库

- 支持导入 PDF、DOCX、PPTX、Markdown 和 TXT。
- 资料复制到当前工作区长期保存，可定位或移入系统回收站。
- 按需提取 PDF 页面、PPTX 幻灯片及 DOCX 段落文本后发送给模型。
- 单个资料上限为 50 MB，单次提取文本上限为 80 万字符。
- 不执行 OCR；扫描版 PDF 或纯图片资料暂时无法提取文字。

### AI 配置、安全与隐私

- 支持 OpenAI 兼容的 Chat Completions 服务地址、模型名称和 API Key。
- API Key 使用 Electron `safeStorage` 保护，不暴露给渲染页面。
- 只有勾选“允许发送所选内容”后，才会发送资料文本、导图上下文和对话。
- 知图不提供内置账号、云端代理或模型请求中转。

## 兼容性与升级

- 从 v0.2.0 可直接升级，无需迁移现有工作区。
- 导图数据结构和 `workspace.json` 版本保持兼容，原有导图、资源、历史快照和导出文件不受影响。
- AI 会话新增资料选择归一化，但文件结构版本仍为 v1，旧会话可直接读取。

## 发布产物

执行 `npm run package:win` 后将生成：

- `release\知图 Setup 0.2.1.exe`：Windows x64 安装程序。
- `release\知图 0.2.1.exe`：Windows x64 免安装便携版。
- `release\win-unpacked`：解包后的应用目录。

SHA-256：

```text
C44BEEDEC88E84032990EB4BC950C25DFA058C7294996DB24C07D3DD2CF69B5B  知图 0.2.1.exe
D5216D7B51E45949B41C6F70554F8ED0DB1B2DC7AED27661AF74BA31914FAD86  知图 Setup 0.2.1.exe
```

## 验证记录

- `npm run typecheck`：通过。
- `npm test`：5 个测试文件、20 项测试全部通过。
- `npm run build`：通过。
- `npm run test:e2e`：2 项端到端测试全部通过，覆盖资料多选、资料内容传递、AI 追问、生成预览、创建新导图及原有工作区编辑流程。

## 已知边界

- AI 功能仅支持 OpenAI 兼容的 Chat Completions 接口，不包含 Responses API 或平台专用接口适配。
- AI 输出质量取决于所选模型和服务商，生成后请先检查预览再应用。
- 不支持图片 OCR、扫描版 PDF 文字识别、音视频资料解析。
- 当前版本仍不包含账号、云同步、多人协作、复习提醒、跨导图引用以及 XMind/FreeMind 互导。
