# 知图 0.2.0 发布说明

> 发布日期：2026-09-17  
> 适用平台：Windows x64  
> 升级基线：v0.1.0

知图 0.2.0 聚焦“从课程资料到可编辑导图”的 AI 工作流。本版新增 OpenAI 兼容的 AI 导图助手、可复用课程资料库、可编辑大纲预览，以及 API Key 加密存储和数据发送确认等控制；原有工作区、导图编辑和导出能力保持兼容。

## 版本亮点

- 新增 **AI 导图助手**，支持从对话需求直接生成新导图，或补充到当前选中的节点。
- 新增 **课程资料库**，可导入并复用 PDF、DOCX、PPTX、Markdown 和 TXT 资料。
- AI 生成结果先进入可编辑预览，确认后再创建导图或以追加、智能合并、替换等方式应用。
- AI 会话持久化到工作区，但不写入导图正文，也不会进入导图导出文件。
- API Key 使用 Electron `safeStorage` 保护，并在发送资料或导图上下文前要求用户明确确认。

## 新增功能

### AI 导图助手

- 支持“新建导图”和“补充选中节点”两种模式。
- 支持先由 AI 追问关键方向，再根据用户回答生成大纲；澄清信息不足时可直接按当前判断继续。
- 支持精简、标准、详尽三档生成规模。
- 补充节点时默认识别当前节点的局部上下文，也可选择包含整张导图。
- 生成过程显示提取资料、分析需求、生成大纲等进度，并支持中途取消。
- 生成结果可编辑根主题标题和摘要，可调整每个分支的标题、摘要及是否纳入大纲。
- 应用预览时支持：
  - 追加且不覆盖：将新分支添加到现有子树。
  - 按标题智能合并：合并同名主题并补充缺失分支。
  - 替换现有子树：删除目标节点原子树并替换，可通过 `Ctrl+Z` 撤销。
- 新建导图模式下可直接创建一张新导图，并将预览内容写入根主题。
- AI 生成期间会暂时锁定切换、新建、删除导图及关闭工作区等操作，避免任务与上下文错位。
- AI 会话保存在 `maps/<mapId>.ai-session.json`，删除导图时会一并移入系统回收站。

### 课程资料库

- 支持导入 PDF、DOCX、PPTX、Markdown 和 TXT。
- 资料复制到当前工作区长期保存，可查看、在文件管理器中定位或移入系统回收站。
- 生成时只选择需要的资料，并按需提取文本后发送给已配置的模型服务。
- 支持按页读取 PDF 文本，按幻灯片顺序读取 PPTX 文本，并解析 DOCX 段落。
- 单个资料上限为 50 MB，单次提取文本上限为 80 万字符。
- 不执行 OCR；扫描版 PDF 或纯图片资料暂时无法提取文字。

### AI 配置、安全与隐私

- 支持配置 OpenAI 兼容的 Chat Completions 服务地址、模型名称和 API Key。
- 支持直接填写服务根地址或完整的 `/chat/completions` 地址，也支持无 API Key 的本地服务。
- 提供“测试连接”功能，便于在生成前检查配置。
- API Key 不会暴露给渲染页面；系统加密能力可用时使用 `safeStorage` 持久化，否则仅在当前进程内保存。
- 只有勾选“允许发送所选内容”后，才会把所选资料文本、导图上下文和对话发送到用户填写的服务地址。
- 知图不提供内置账号、云端代理，也不接管模型请求；数据直接发送给用户配置的服务。

## 兼容性与升级

- 从 v0.1.0 可直接升级，无需迁移现有工作区。
- 导图数据结构和 `workspace.json` 版本保持兼容，原有导图、资源、历史快照和导出文件不受影响。
- AI 会话文件分别存放，不影响旧版工作区读取；首次使用 AI 相关功能时按需创建。
- 当前安装包未进行代码签名，Windows 可能显示 SmartScreen 或“未知发布者”提示。请优先核对下方 SHA-256。

## 发布产物

- `release\知图 Setup 0.2.0.exe`：Windows x64 安装程序。
- `release\知图 0.2.0.exe`：Windows x64 免安装便携版。
- `release\win-unpacked`：解包后的应用目录。

SHA-256：

```text
EC9D77DC02C39674D69D34A3DEEEAD55F26E08F1A2BC233F1506904199812E11  知图 0.2.0.exe
4D658ACE7117673F21D2A5D63C401AB452DE2D5CD842C1C2C3E0B87AC5D5CF30  知图 Setup 0.2.0.exe
```

## 验证记录

- `npm run typecheck`：通过。
- `npm test`：5 个测试文件、19 项测试全部通过。
- `npm run test:e2e`：2 项端到端测试全部通过，覆盖 AI 追问、生成预览、创建新导图及原有工作区编辑流程。

## 已知边界

- AI 功能仅支持 OpenAI 兼容的 Chat Completions 接口，不包含 Responses API 或平台专用接口适配。
- AI 输出质量取决于所选模型和服务商，生成后请先检查预览再应用。
- 不支持图片 OCR、扫描版 PDF 文字识别、音视频资料解析。
- 当前版本仍不包含账号、云同步、多人协作、复习提醒、跨导图引用以及 XMind/FreeMind 互导。
