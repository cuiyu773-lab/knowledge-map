# 知图 0.4.0 发布说明

> 发布日期：2026-09-18  
> 适用平台：Windows x64  
> 升级基线：v0.3.0

知图 0.4.0 是导出清晰度增强版。本版为 PNG 和 PDF 增加标准、高清、超清三档导出画质，重写了自适应裁剪尺寸计算，并继续对 PDF 使用高质量 JPEG 压缩，在可读性和文件体积之间提供可控选择。

## 0.4.0 更新

### 导出画质档位

- PNG 和 PDF 新增“标准 / 高清 / 超清”三档画质，默认使用高清。
- PNG 与 PDF 共用当前画质档位，导出菜单通过二级菜单选择。
- 画质选择写入用户设置，重启应用后继续使用上次选择。
- SVG 保持矢量导出，不参与栅格画质档位。
- 旧版设置缺少 `exportQuality` 或值非法时，自动回退到高清。

### 高分辨率导出与压缩

- 导出前读取实际节点 DOM 矩形，换算为 React Flow 坐标并计算真实 Bounding Box。
- 画布仅保留 48 像素内容边距，不再使用固定最小尺寸造成大块空白。
- PNG 最大输出限制为超清档 `3× / 8192px / 4800 万像素`，大型导图自动等比缩放。
- PDF 使用高质量 JPEG：标准 `0.82`、高清 `0.88`、超清 `0.90`，并启用 jsPDF 流压缩。
- PDF 根据导图方向自动选择 A3 横向或纵向。
- 分辨率上限同时约束最大边长和总像素，降低超大导出导致内存溢出的风险。

### 兼容性

- 工作区和导图数据结构保持 v1，无需迁移。
- 原有 PNG、SVG、PDF 导出入口继续可用。
- 旧版本设置文件可直接读取，并在首次保存后补齐导出画质字段。

## 0.3.0 功能基线

- PPTX 中的 WMF/EMF 公式和几何图会提取为原始附件及高清 PNG。
- 支持复用 OpenAI 兼容配置，将视觉对象批量转为 LaTeX 或结构化 Markdown。
- 视觉识别结果按图片哈希、模型和提示词版本缓存。
- 原有课程资料多选、AI 追问、预览和导图生成能力保持可用。

## 发布产物

- `release\知图 Setup 0.4.0.exe`：Windows x64 安装程序，约 151.1 MiB。
- `release\知图 0.4.0.exe`：Windows x64 免安装便携版，约 150.9 MiB。
- `release\win-unpacked\resources\metafile-renderer.exe`：内置 WMF/EMF 渲染器。

SHA-256：

```text
B4A010C8D72C55CBA7EF509088433CEC1DD3867C87A68D976DB30D3680DF1C24  知图 Setup 0.4.0.exe
9C70EE4AA18A4F03143083EB07AD71D156BF3517791D87236B0AAA69889BE2DF  知图 0.4.0.exe
```

## 验证记录

- `npm run typecheck`：通过。
- `npm test`：10 个测试文件、33 项测试全部通过。
- `npm run build`：通过。
- `npm run test:e2e`：2 项端到端测试全部通过。
- Windows x64 NSIS 安装版和便携版打包成功。
- 测试导图实际 PNG 尺寸：标准 `1170×348`、高清 `1462×435`、超清 `1755×522`。
- 高清 PDF 嵌入 JPEG 实测为 `1023×304`，设置持久化字段为 `exportQuality: "ultra"`。

## 已知边界

- 当前安装包未进行商业代码签名，Windows 可能显示 SmartScreen 或“未知发布者”提示。请先核对本说明中的 SHA-256。
- 超清档位会明显增加内存占用、导出时间和文件体积，请根据导图规模选择。
- 极长导图仍会缩放至单页 A3；如需逐页打印，可使用后续分页导出能力。
- 模型识别结果可能存在误差，公式和几何关系应结合保留的原图复核。
- 不支持扫描版 PDF 的本地 OCR、音视频资料解析、账号、云同步、多人协作或 XMind/FreeMind 互导。
